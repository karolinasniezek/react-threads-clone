Threads App clone in React based on the tutorial.

### Running the app
### App development

To start the app run `npm start`. Open your browser and navigate to http://localhost:3000/.

To start the server run `npx json-server db.json`
